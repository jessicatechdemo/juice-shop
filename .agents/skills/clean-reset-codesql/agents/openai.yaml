interface:
  display_name: "Clean Reset CodeSQL"
  short_description: "Reset CodeQL analyses and retriage safely"
  default_prompt: "Use $clean-reset-codesql to preview and run an approval-gated CodeQL branch reset, rescan, and retriage."
policy:
  allow_implicit_invocation: false
