interface:
  display_name: "Triage CodeQL and Store"
  short_description: "Triage CodeQL into tracked GitHub comments"
  default_prompt: "Use $triage-codeql-and-store to triage CodeQL alerts on my current branch, save a filterable report, and preview GitHub tracking issue comments without dismissing alerts."
policy:
  allow_implicit_invocation: false
